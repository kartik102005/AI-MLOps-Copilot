---
phase: 02-user-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/contexts/AuthContext.tsx
  - frontend/src/pages/ProfilePage.tsx
  - frontend/src/components/profile/ProfileView.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [PROF-01, PROF-02]
user_setup: []

estimate:
  tokens: 12000
  raw_tokens: 8000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "User can view their profile name and email on /profile page"
    - "AuthContext exposes updateUser method that refreshes user state after API call"
    - "Profile page is protected — redirects unauthenticated users to /login"
    - "Profile page uses centered card layout matching login/register style"
  artifacts:
    - frontend/src/contexts/AuthContext.tsx (extended with updateUser)
    - frontend/src/pages/ProfilePage.tsx
    - frontend/src/components/profile/ProfileView.tsx
  key_links:
    - "AuthContext.updateUser → supabase.auth.updateUser → supabase.auth.getUser → setUser"
    - "App.tsx /profile route → ProtectedRoute → ProfilePage"
---

<objective>
Extend AuthContext with updateUser method and create ProfilePage with read-only view mode.

Purpose: Establishes the auth state update mechanism that all profile operations depend on, plus a working profile page users can navigate to.
Output: AuthContext with updateUser, ProfilePage with ProfileView component, /profile route wired in App.tsx.
</objective>

<execution_context>
@C:/Users/karti/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/karti/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-user-profile/02-CONTEXT.md
@.planning/phases/02-user-profile/02-RESEARCH.md
@frontend/src/contexts/AuthContext.tsx
@frontend/src/App.tsx
@frontend/src/pages/LoginPage.tsx
@frontend/src/components/auth/ProtectedRoute.tsx
@frontend/src/components/auth/LoginForm.tsx
@frontend/src/lib/supabase.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Extend AuthContext with updateUser method</name>
  <files>frontend/src/contexts/AuthContext.tsx</files>
  <read_first>
    - frontend/src/contexts/AuthContext.tsx (current AuthContext with user, session, loading, signUp, signIn, signOut)
    - frontend/src/lib/supabase.ts (createSupabaseClient pattern)
  </read_first>
<action>
Add updateUser method to AuthContextType interface and AuthProvider. The method signature must be:
`updateUser: (updates: { data?: { full_name?: string; email?: string }; password?: string }) => Promise<{ error: string | null }>`

Implementation per D-11 (auto-update AuthContext user object immediately after save):
1. Call `supabase.auth.updateUser(updates)` — do NOT call refreshSession() after this (research pitfall: causes "Invalid Refresh Token" error)
2. On success, call `supabase.auth.getUser()` to fetch fresh user data (research recommendation: use getUser, not refreshSession)
3. Call `setUser(refreshedUser)` to update local state immediately
4. Return `{ error: null }` on success, `{ error: error.message }` on failure

Export the updateUser in the Provider value object so consuming components can call it.

Important: Keep loading state management consistent with existing signUp/signIn patterns (setLoading true at start, false at end).
</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AuthContextType interface includes `updateUser` with signature `(updates: { data?: { full_name?: string; email?: string }; password?: string }) => Promise<{ error: string | null }>`
    - AuthProvider value includes updateUser in the Provider
    - updateUser calls supabase.auth.updateUser then supabase.auth.getUser (not refreshSession)
    - updateUser calls setUser with the refreshed user from getUser
    - No TypeScript errors in AuthContext.tsx
  </acceptance_criteria>
  <done>AuthContext exposes updateUser method that updates user state immediately after Supabase API call. TypeScript compiles without errors.</done>
  <reversibility rating="reversible">New method added to existing context; no existing code changed.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Create ProfileView component</name>
  <files>frontend/src/components/profile/ProfileView.tsx</files>
  <read_first>
    - frontend/src/components/auth/LoginForm.tsx (Tailwind patterns: rounded-md, shadow-sm, indigo focus rings, disabled states)
    - frontend/src/pages/LoginPage.tsx (centered card layout pattern: flex min-h-screen items-center justify-center bg-gray-50)
    - frontend/src/contexts/AuthContext.tsx (user object shape: user.user_metadata.full_name, user.email)
  </read_first>
  <action>
Create ProfileView component per D-05 (view mode with Edit toggle) and D-04 (name and email only).

Component receives props: `{ user: User; onEdit: () => void }`

Display read-only profile information:
- Full Name: read from `user.user_metadata.full_name` (fallback to "Not set" if empty)
- Email: read from `user.email`

Layout per D-01 (centered card matching login/register style):
- Section heading "Profile Information" with indigo accent
- Two rows: label (text-sm font-medium text-gray-500) + value (text-base text-gray-900)
- "Edit" button at bottom right: `rounded-md bg-indigo-600 px-4 py-2 text-white font-medium hover:bg-indigo-700`
- Button calls onEdit prop when clicked

Use the same Tailwind classes as LoginForm for consistency: rounded-md borders, indigo focus rings, shadow-sm inputs.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ProfileView.tsx exists in frontend/src/components/profile/
    - Exports `ProfileView` function component
    - Accepts `{ user: User; onEdit: () => void }` props
    - Displays user.user_metadata.full_name (with "Not set" fallback)
    - Displays user.email
    - Has "Edit" button that calls onEdit
    - Uses Tailwind classes matching LoginForm patterns
    - No TypeScript errors
  </acceptance_criteria>
  <done>ProfileView renders user name and email in read-only mode with an Edit button.</done>
</task>

<task type="auto">
  <name>Task 3: Create ProfilePage and wire route</name>
  <files>frontend/src/pages/ProfilePage.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (current Routes setup with ProtectedRoute pattern)
    - frontend/src/pages/LoginPage.tsx (page structure pattern: flex min-h-screen, centered card)
    - frontend/src/contexts/AuthContext.tsx (useAuth hook for accessing user)
    - frontend/src/components/auth/ProtectedRoute.tsx (route protection pattern)
  </read_first>
  <action>
Create ProfilePage.tsx following LoginPage pattern per D-01 (standalone /profile page with centered card layout):

ProfilePage structure:
1. Import useAuth to get user
2. Render centered card layout: `flex min-h-screen items-center justify-center bg-gray-50 px-4 py-12`
3. Card: `w-full max-w-md space-y-8` with heading "My Profile" and `rounded-lg bg-white p-8 shadow-md`
4. Inside card: render ProfileView with user prop and onEdit handler
5. State: `isEditing` useState(false) — toggles between ProfileView and ProfileEdit (placeholder for now)
6. For now, ProfileEdit is a placeholder div saying "Edit mode coming in Plan 2"

Wire route in App.tsx:
1. Import ProfilePage
2. Add route: `<Route path="/profile" element={<ProtectedRoute><ProfilePage /></ProtectedRoute>} />`
3. Place it after the /dashboard route, before the catch-all route

Per D-02: Profile is accessible via user menu dropdown — this is a separate navigation concern (Plan 2). The route itself is wired here.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ProfilePage.tsx exists in frontend/src/pages/
    - Exports `ProfilePage` function component
    - Renders centered card layout with "My Profile" heading
    - Renders ProfileView with user from useAuth()
    - Has isEditing state toggle (default false)
    - App.tsx has /profile route wrapped in ProtectedRoute
    - /profile redirects to /login for unauthenticated users
    - No TypeScript errors
  </acceptance_criteria>
  <done>ProfilePage at /profile displays user profile in read-only mode. Unauthenticated users redirected to login.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→Supabase | User metadata updates cross from browser to Supabase Auth API |
| Client→UI | User input (profile edits) crosses from form to state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | AuthContext.updateUser | low | accept | Supabase validates metadata server-side; client cannot inject arbitrary fields |
| T-02-02 | Information Disclosure | ProfileView | low | accept | Profile only shows current user's own data; ProtectedRoute prevents unauthenticated access |
| T-02-SC | Tampering | npm packages | high | mitigate | All packages from Phase 1 already installed; no new packages in this plan |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — no TypeScript errors
- Profile page renders at /profile with user name and email
- AuthContext.updateUser method exists and is callable
- Unauthenticated visit to /profile redirects to /login
</verification>

<success_criteria>
1. AuthContext exposes updateUser method that refreshes user state via getUser (not refreshSession)
2. ProfilePage at /profile shows user name and email in read-only mode
3. Profile page uses same centered card layout as login/register
4. Route is protected — unauthenticated users redirected to /login
</success_criteria>

<output>
Create `.planning/phases/02-user-profile/02-01-SUMMARY.md` when done
</output>
