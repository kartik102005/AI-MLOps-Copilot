---
phase: 05-ci-cd-pipeline-generation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/components/projects/ProjectTabs.tsx
  - frontend/src/components/cicd/CICDEditor.tsx
  - frontend/src/components/cicd/CICDToolbar.tsx
  - frontend/src/components/cicd/ValidationErrors.tsx
  - frontend/package.json
autonomous: true
requirements: [CICD-03, CICD-04]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 22000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "CI/CD Pipeline tab appears in ProjectTabs with IconActivity icon"
    - "Two stacked CodeMirror YAML editors display CI and CD workflows simultaneously"
    - "actionlint validation errors display with line numbers and error codes"
    - "Separate download buttons for CI workflow (ci.yml) and CD workflow (cd.yml)"
    - "Regenerate button triggers POST /api/cicd/generate and updates both editors"
    - "Tab uses same layout pattern as Docker tab per D-22"
  artifacts:
    - path: "frontend/src/components/cicd/CICDEditor.tsx"
      provides: "YAML editor with syntax highlighting and error display"
      exports: ["CICDEditor"]
    - path: "frontend/src/components/cicd/CICDToolbar.tsx"
      provides: "Download and regenerate controls for CI/CD workflows"
      exports: ["CICDToolbar"]
    - path: "frontend/src/components/cicd/ValidationErrors.tsx"
      provides: "Actionlint error display with line references"
      exports: ["CICDEditorValidationErrors"]
    - path: "frontend/src/components/projects/ProjectTabs.tsx"
      provides: "CI/CD Pipeline tab integrated into project detail view"
  key_links:
    - from: "frontend/src/components/projects/ProjectTabs.tsx"
      to: "frontend/src/components/cicd/CICDEditor.tsx"
      via: "CICDEditor imported and rendered in CI/CD tab content"
      pattern: "import.*CICDEditor"
    - from: "frontend/src/components/projects/ProjectTabs.tsx"
      to: "frontend/src/components/cicd/CICDToolbar.tsx"
      via: "CICDToolbar imported and rendered above editors"
      pattern: "import.*CICDToolbar"
    - from: "frontend/src/components/projects/ProjectTabs.tsx"
      to: "backend/app/api/cicd.py"
      via: "fetchApi('/api/cicd/generate', ...) called on generate button click"
      pattern: "api/cicd/generate"
---

<objective>
Add CI/CD Pipeline tab to ProjectTabs with dual YAML editors (CI + CD stacked), actionlint validation error display, and download controls. Reuses CodeMirror 6 from existing DockerfileEditor pattern.

Purpose: Users need a UI to view, edit, and download generated CI/CD workflows. This plan delivers the core editor experience per D-21, D-22, D-23, D-04, D-19.
Output: 5 files — 3 new components, 1 modified tab, 1 package.json update
</objective>

<execution_context>
@C:/Users/karti/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/karti/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-cd-pipeline-generation/05-CONTEXT.md
@.planning/phases/05-ci-cd-pipeline-generation/05-RESEARCH.md

# Pattern sources — read before implementing
@frontend/src/components/docker/DockerfileEditor.tsx
@frontend/src/components/docker/DockerfileToolbar.tsx
@frontend/src/components/docker/ValidationErrors.tsx
@frontend/src/components/projects/ProjectTabs.tsx
@frontend/src/App.tsx

# Depends on Plan 1 backend
@.planning/phases/05-ci-cd-pipeline-generation/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CICDEditor + CICDToolbar + ValidationErrors components</name>
  <files>frontend/src/components/cicd/CICDEditor.tsx, frontend/src/components/cicd/CICDToolbar.tsx, frontend/src/components/cicd/ValidationErrors.tsx</files>
  <read_first>
    - frontend/src/components/docker/DockerfileEditor.tsx (CodeMirror pattern to replicate)
    - frontend/src/components/docker/DockerfileToolbar.tsx (toolbar pattern)
    - frontend/src/components/docker/ValidationErrors.tsx (error display pattern)
  </read_first>
  <action>
First install @codemirror/lang-yaml: run "npm install @codemirror/lang-yaml" in frontend/ directory.

Create three components in frontend/src/components/cicd/:

**CICDEditor.tsx** — YAML editor component:
- Interface: { value: string; onChange: (value: string) => void; errors?: Array<{line: number; message: string; level: string}>; label?: string }
- Use CodeMirror from @uiw/react-codemirror with @codemirror/lang-yaml for YAML syntax highlighting (yaml() extension)
- Import yaml from "@codemirror/lang-yaml"
- Extensions: [yaml(), EditorView.lineWrapping, EditorState.tabSize.of(2)]
- Theme: "dark", same basicSetup as DockerfileEditor
- Error line highlight overlay banner when errors.length > 0
- Inline error tooltips at bottom showing line numbers and messages
- Same styling classes as DockerfileEditor: rounded-xl border border-border-light bg-surface

**CICDToolbar.tsx** — Download and regenerate controls:
- Interface: { ciWorkflow: string; cdWorkflow: string; onRegenerate: () => void; isGenerating: boolean; ciErrors: Array; cdErrors: Array; onDownload: (type: "ci" | "cd") => void }
- Two-section layout: CI Workflow controls on left, CD Workflow controls on right
- Each section: label ("CI Workflow" / "CD Workflow"), download button, error count badge
- Download buttons: call onDownload("ci") and onDownload("cd") respectively
- Regenerate button: calls onRegenerate, shows spinner when isGenerating per D-04
- Error badges: show count of validation errors per workflow type per D-07

**ValidationErrors.tsx** — Actionlint error display (same as Docker ValidationErrors):
- Interface: { errors: Array<{line: number; code: string; message: string; level: string}>; onLineClick?: (line: number) => void }
- Group errors by level, show count badges for errors and warnings
- Each error shows line number, code, and message per D-07
- Green "No validation errors" state when empty per D-08
- Reuse the exact same LEVEL_STYLES and layout as Docker ValidationErrors component
  </action>
  <verify>
    <automated>cd C:/Users/karti/OneDrive/Desktop/KARTIK/frontend; npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - @codemirror/lang-yaml installed in frontend/package.json
    - CICDEditor renders CodeMirror with YAML syntax highlighting
    - CICDToolbar shows CI/CD labels, download buttons, regenerate button, error badges
    - ValidationErrors groups errors by level with line numbers
    - All three components export correctly and TypeScript compiles
  </acceptance_criteria>
  <done>Three CI/CD frontend components created with YAML editor, toolbar, and validation display.</done>
</task>

<task type="auto">
  <name>Task 2: CI/CD Pipeline tab in ProjectTabs</name>
  <files>frontend/src/components/projects/ProjectTabs.tsx</files>
  <read_first>
    - frontend/src/components/projects/ProjectTabs.tsx (existing tab structure)
    - frontend/src/components/cicd/CICDEditor.tsx (component created in Task 1)
    - frontend/src/components/cicd/CICDToolbar.tsx (component created in Task 1)
    - frontend/src/components/cicd/ValidationErrors.tsx (component created in Task 1)
  </read_first>
  <action>
Add CI/CD Pipeline tab to ProjectTabs per D-21, D-22, D-23, D-04, D-19:

1. Add imports at top:
   - import { CICDEditor } from '../cicd/CICDEditor'
   - import { CICDToolbar } from '../cicd/CICDToolbar'
   - import { CICDEditorValidationErrors } from '../cicd/ValidationErrors' (or just import as ValidationErrors from the cicd folder)

2. Add state variables:
   - ciWorkflow: string = '' (initialized from project.cicd_config?.ci_workflow || '')
   - cdWorkflow: string = '' (initialized from project.cicd_config?.cd_workflow || '')
   - ciValidationErrors: ValidationError[] = []
   - cdValidationErrors: ValidationError[] = []
   - isGeneratingCICD: boolean = false
   - cicdError: string | null = null

3. Add "cicd" to the activeTab union type: 'overview' | 'files' | 'analysis' | 'docker' | 'cicd' | 'settings'

4. Add tab entry to tabs array:
   { id: 'cicd', label: 'CI/CD Pipeline', Icon: IconActivity }

5. Add handleGenerateCICD async function:
   - POST to /api/cicd/generate with { project_id: project.id }
   - On success: set ciWorkflow, cdWorkflow, ciValidationErrors, cdValidationErrors from response
   - Call onUpdateProject with updated cicd_config
   - On error: set cicdError with error message per D-09

6. Add handleDownloadCICD function:
   - GET /api/cicd/download/{project.id}/{type} where type is "ci" or "cd"
   - Create blob from response text, create object URL, trigger download via anchor click
   - Filename: "ci.yml" or "cd.yml" per D-12, D-19

7. Add CI/CD tab content section (when activeTab === 'cicd'):
   - Same header layout as Docker tab per D-22: rounded-2xl border, flex with icon and title
   - Header text: "CI/CD Pipeline Generator" with IconActivity icon
   - Subtitle: "Generate, validate, and download GitHub Actions CI/CD workflows"
   - Generate button: "Generate Workflows" / "Regenerate Workflows" per D-04
   - cicdError display if present per D-09
   - When workflows exist: CICDToolbar + two stacked CICDEditor sections (CI on top, CD below) per D-23
   - Each editor labeled "CI Workflow" and "CD Workflow" respectively
   - ValidationErrors for each workflow below its editor per D-07, D-08
   - Empty state when no workflows: dashed border, IconActivity icon, "No CI/CD workflows generated yet" message, generate button per D-03
  </action>
  <verify>
    <automated>cd C:/Users/karti/OneDrive/Desktop/KARTIK/frontend; npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - "CI/CD Pipeline" tab with IconActivity icon appears in tab navigation
    - Clicking tab shows CI/CD Pipeline Generator page with generate button
    - Generate button calls POST /api/cicd/generate
    - Two stacked YAML editors display CI and CD workflows
    - Validation errors display below each editor with line numbers
    - Separate download buttons for ci.yml and cd.yml
    - Empty state shows when no workflows generated
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>CI/CD Pipeline tab fully integrated into ProjectTabs with editors, validation, and download per D-21 through D-24.</done>
</task>

<task type="auto">
  <name>Task 3: Wire download handler and verify end-to-end flow</name>
  <files>frontend/src/components/projects/ProjectTabs.tsx</files>
  <read_first>
    - frontend/src/components/projects/ProjectTabs.tsx (file modified in Task 2)
  </read_first>
  <action>
Final integration polish for the CI/CD tab:

1. Ensure the download handler works correctly:
   - handleDownloadCICD("ci") fetches GET /api/cicd/download/{project.id}/ci
   - handleDownloadCICD("cd") fetches GET /api/cicd/download/{project.id}/cd
   - Response is plain text YAML, create Blob with type "text/yaml"
   - Use URL.createObjectURL + anchor.click pattern for download
   - Revoke object URL after download
   - Download filename: "ci.yml" or "cd.yml"

2. Ensure Stage 04 in Overview tab shows CI/CD status per D-24:
   - Stage 04 "CI/CD Deploy" should show "Workflow Ready" (green) when project.cicd_config exists
   - Should show "Pending Pipeline" (gray) when cicd_config is null
   - This is already partially implemented in the existing Overview tab — verify it reads project.cicd_config correctly

3. Ensure cicd_config persists across tab switches:
   - When user generates workflows, cicd_config is stored via onUpdateProject
   - When user switches tabs and comes back, ciWorkflow/cdWorkflow are initialized from project.cicd_config

4. Verify the full flow works:
   - User clicks "Generate Workflows" → POST /api/cicd/generate → workflows appear in editors
   - User clicks "Download CI" → GET /api/cicd/download/{id}/ci → ci.yml downloaded
   - User clicks "Download CD" → GET /api/cicd/download/{id}/cd → cd.yml downloaded
   - User clicks "Regenerate" → workflows are regenerated and editors update
   - Validation errors display below each editor
  </action>
  <verify>
    <automated>cd C:/Users/karti/OneDrive/Desktop/KARTIK/frontend; npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Download handler creates proper blob URLs and triggers file downloads
    - ci.yml and cd.yml download with correct filenames
    - cicd_config persists across tab switches
    - Overview Stage 04 reflects cicd_config status per D-24
    - TypeScript compiles without errors
    - Full generate → edit → download flow works end-to-end
  </acceptance_criteria>
  <done>CI/CD Pipeline tab fully functional with generate, edit, validate, download, and status indicator. Phase 5 core features complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend→Backend | Workflow YAML content sent to API for generation and validation |
| User→Editor | User-edited YAML content may contain security issues |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | User-edited workflow YAML | medium | accept | User edits their own workflow; actionlint validates for common issues. Generated workflows follow security best practices. |
| T-05-05 | Information Disclosure | Download endpoint | low | accept | Downloads are user's own workflow files. No other user's data exposed. |
</threat_model>

<verification>
- CI/CD Pipeline tab renders in ProjectTabs with IconActivity icon
- Two YAML editors display with syntax highlighting
- Download buttons produce correctly-named files (ci.yml, cd.yml)
- Validation errors display with line numbers
- TypeScript compiles without errors
</verification>

<success_criteria>
- CI/CD Pipeline tab accessible from project detail page per D-21
- Two stacked YAML editors for CI and CD workflows per D-23
- actionlint validation errors displayed per D-07
- Download buttons for ci.yml and cd.yml per D-19
- Regenerate button triggers AI regeneration per D-04, D-09
- Same layout as Docker tab per D-22
</success_criteria>

<output>
Create `.planning/phases/05-ci-cd-pipeline-generation/05-02-SUMMARY.md` when done
</output>
