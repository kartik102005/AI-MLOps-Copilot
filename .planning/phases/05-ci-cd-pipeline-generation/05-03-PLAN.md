---
phase: 05-ci-cd-pipeline-generation
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - frontend/src/components/projects/ProjectTabs.tsx
  - frontend/src/components/cicd/CICDCustomizeForm.tsx
  - frontend/src/components/cicd/SecretsDisplay.tsx
autonomous: true
requirements: [CICD-03]
user_setup: []

estimate:
  tokens: 20000
  raw_tokens: 15000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Collapsible Customize section above editors with trigger, version, and extras options"
    - "AI regeneration incorporates user customization choices per D-15"
    - "Required Secrets section shows DOCKERHUB_USERNAME and DOCKERHUB_TOKEN with setup instructions per D-20"
    - "Overview tab Stage 04 becomes green/active when cicd_config exists per D-24"
    - "Customization choices persist during session and apply to regeneration"
  artifacts:
    - path: "frontend/src/components/cicd/CICDCustomizeForm.tsx"
      provides: "Collapsible customization form for workflow triggers, versions, extras"
      exports: ["CICDCustomizeForm"]
    - path: "frontend/src/components/cicd/SecretsDisplay.tsx"
      provides: "Required GitHub secrets display with setup instructions"
      exports: ["SecretsDisplay"]
  key_links:
    - from: "frontend/src/components/projects/ProjectTabs.tsx"
      to: "frontend/src/components/cicd/CICDCustomizeForm.tsx"
      via: "CICDCustomizeForm rendered above editors in CI/CD tab"
      pattern: "import.*CICDCustomizeForm"
    - from: "frontend/src/components/projects/ProjectTabs.tsx"
      to: "frontend/src/components/cicd/SecretsDisplay.tsx"
      via: "SecretsDisplay rendered below editors in CI/CD tab"
      pattern: "import.*SecretsDisplay"
---

<objective>
Add workflow customization form, required GitHub secrets display, and overview stage indicator polish. Completes the CI/CD Pipeline feature with full user control per D-14, D-15, D-16, D-20, D-24.

Purpose: Users need to customize generated workflows (triggers, versions, extras) and know what GitHub secrets to configure. This plan delivers the remaining user-facing features.
Output: 2 new components, 1 modified tab file
</objective>

<execution_context>
@C:/Users/karti/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/karti/.config/opencode/gsd-core/templates/summary.md
</context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-cd-pipeline-generation/05-CONTEXT.md
@.planning/phases/05-ci-cd-pipeline-generation/05-RESEARCH.md

# Depends on Plans 1 and 2
@.planning/phases/05-ci-cd-pipeline-generation/05-01-SUMMARY.md
@.planning/phases/05-ci-cd-pipeline-generation/05-02-SUMMARY.md

# Pattern sources
@frontend/src/components/projects/ProjectTabs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: CICDCustomizeForm + SecretsDisplay components</name>
  <files>frontend/src/components/cicd/CICDCustomizeForm.tsx, frontend/src/components/cicd/SecretsDisplay.tsx</files>
  <read_first>
    - frontend/src/components/projects/ProjectTabs.tsx (to understand integration point)
  </read_first>
  <action>
Create two new components per D-14, D-16, D-20:

**CICDCustomizeForm.tsx** — Collapsible customization section:
- Interface: { onRegenerate: (customPrompt: string) => void; isGenerating: boolean }
- Collapsible section with chevron toggle per D-16
- Title: "Customize Workflows" with gear icon
- Default collapsed, expands to show form fields
- Form fields per D-14:
  - Triggers: checkboxes for "Push to main", "Pull request to main", "Manual dispatch" (all checked by default)
  - Python version: dropdown with "3.10", "3.11", "3.12" (default "3.12")
  - Node version: dropdown with "18", "20", "22" (default "20")
  - Extra steps: textarea for additional workflow steps (placeholder: "e.g., cache pip dependencies, add code coverage")
  - Environment variables: textarea for env vars (placeholder: "e.g., NODE_ENV=production")
- "Apply & Regenerate" button that builds a custom prompt from form values and calls onRegenerate(customPrompt)
- Custom prompt format: "Triggers: {selected}. Python version: {ver}. Node version: {ver}. Extra steps: {text}. Env vars: {text}."
- Styled with rounded-2xl border, bg-surface, same as other sections

**SecretsDisplay.tsx** — Required GitHub secrets section:
- Interface: { secrets?: Array<{name: string; description: string; required: boolean}> }
- Default secrets list (when prop not provided): DOCKERHUB_USERNAME ("Your Docker Hub username for image registry", true), DOCKERHUB_TOKEN ("Docker Hub access token for pushing images", true)
- Each secret shows: lock icon, secret name (monospace), description, "Required" badge
- Instructions section: "Add these secrets in your GitHub repository: Settings > Secrets and variables > Actions > New repository secret"
- Styled with rounded-2xl border, bg-surface, same as other sections
- Rendered below editors in CI/CD tab per D-20
  </action>
  <verify>
    <automated>cd C:/Users/karti/OneDrive/Desktop/KARTIK/frontend; npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - CICDCustomizeForm renders collapsible section with trigger/version/extras fields
    - CICDCustomizeForm builds custom prompt from form values
    - SecretsDisplay shows DOCKERHUB_USERNAME and DOCKERHUB_TOKEN per D-20
    - SecretsDisplay includes GitHub setup instructions
    - Both components export correctly and TypeScript compiles
  </acceptance_criteria>
  <done>Customization form and secrets display components created.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate customize form + secrets into CI/CD tab</name>
  <files>frontend/src/components/projects/ProjectTabs.tsx</files>
  <read_first>
    - frontend/src/components/projects/ProjectTabs.tsx (current state after Plan 2)
    - frontend/src/components/cicd/CICDCustomizeForm.tsx (created in Task 1)
    - frontend/src/components/cicd/SecretsDisplay.tsx (created in Task 1)
  </read_first>
  <action>
Integrate the new components into the CI/CD Pipeline tab per D-14, D-15, D-16, D-20:

1. Add imports:
   - import { CICDCustomizeForm } from '../cicd/CICDCustomizeForm'
   - import { SecretsDisplay } from '../cicd/SecretsDisplay'

2. Add state for custom prompt:
   - customPrompt: string = ''

3. Modify handleGenerateCICD to accept optional customPrompt parameter:
   - If customPrompt is provided, include it in the POST body: { project_id: project.id, custom_prompt: customPrompt }
   - This enables the customize form to pass user choices to the AI per D-15

4. Add wrapper function for customize form:
   - handleCustomRegenerate = (prompt: string) => { setCustomPrompt(prompt); handleGenerateCICD(prompt); }

5. Add CICDCustomizeForm to CI/CD tab content:
   - Place ABOVE the editors (before CICDToolbar) per D-16
   - Pass onRegenerate={handleCustomRegenerate} and isGenerating={isGeneratingCICD}

6. Add SecretsDisplay to CI/CD tab content:
   - Place BELOW the editors (after ValidationErrors) per D-20
   - Use default secrets list (no prop needed)

7. Ensure the layout order is:
   - Header + Generate button
   - CICDCustomizeForm (collapsible)
   - CICDToolbar
   - CICDEditor (CI workflow) + ValidationErrors
   - CICDEditor (CD workflow) + ValidationErrors
   - SecretsDisplay
  </action>
  <verify>
    <automated>cd C:/Users/karti/OneDrive/Desktop/KARTIK/frontend; npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - CICDCustomizeForm appears above editors in CI/CD tab per D-16
    - SecretsDisplay appears below editors per D-20
    - Customize form passes custom prompt to generate endpoint per D-15
    - Layout order: header, customize form, toolbar, CI editor, CD editor, secrets
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Customization form and secrets display integrated into CI/CD tab. Full layout complete.</done>
</task>

<task type="auto">
  <name>Task 3: Overview Stage 04 indicator + final polish</name>
  <files>frontend/src/components/projects/ProjectTabs.tsx</files>
  <read_first>
    - frontend/src/components/projects/ProjectTabs.tsx (current state after Task 2)
  </read_first>
  <action>
Final polish per D-24:

1. Verify Stage 04 "CI/CD Deploy" in Overview tab:
   - When project.cicd_config exists: show green background (bg-success-light border-success/20), green text (text-success), "Workflow Ready" status
   - When project.cicd_config is null: show gray background (bg-gray-50 border-border-light opacity-60), gray text (text-text-muted), "Pending Pipeline" status
   - This should already be implemented from the existing code. Verify it reads project.cicd_config correctly.

2. Add a "CI/CD Pipeline" link in Stage 04 card when cicd_config exists:
   - When user clicks the Stage 04 card and cicd_config is present, switch to the 'cicd' tab
   - onClick handler: setActiveTab('cicd')
   - Visual cue: cursor-pointer and hover effect when cicd_config exists per D-24

3. Ensure the full feature set is complete:
   - Generate button triggers POST /api/cicd/generate per D-03, D-04
   - Two stacked YAML editors for CI and CD per D-23
   - Validation errors display per D-07, D-08
   - Download buttons for ci.yml and cd.yml per D-19
   - Customize form with triggers, versions, extras per D-14, D-16
   - Secrets display with DOCKERHUB_USERNAME and DOCKERHUB_TOKEN per D-20
   - Regenerate with custom prompt per D-15
   - Overwrite on regenerate per D-17
   - Overview Stage 04 active when cicd_config exists per D-24
  </action>
  <verify>
    <automated>cd C:/Users/karti/OneDrive/Desktop/KARTIK/frontend; npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Stage 04 in Overview tab reflects cicd_config status per D-24
    - Stage 04 card is clickable and navigates to CI/CD tab when cicd_config exists
    - All 15 user decisions (D-01 through D-24) are implemented
    - TypeScript compiles without errors
    - Full feature set matches CONTEXT.md requirements
  </acceptance_criteria>
  <done>CI/CD Pipeline Generation phase complete. All user decisions implemented, all requirements covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User→CustomizeForm | User input flows into AI prompt for regeneration |
| User→Editor | User-edited YAML may contain insecure patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Tampering | Custom prompt injection | low | accept | Custom prompt is user's own input to their own AI generation. No cross-user risk. |
| T-05-07 | Information Disclosure | SecretsDisplay | low | accept | SecretsDisplay shows generic secret names, not actual values. User configures in GitHub. |
</threat_model>

<verification>
- CICDCustomizeForm renders with collapsible section and form fields
- SecretsDisplay shows DOCKERHUB_USERNAME and DOCKERHUB_TOKEN with instructions
- Overview Stage 04 reflects cicd_config status
- All TypeScript compiles without errors
</verification>

<success_criteria>
- Collapsible customize section per D-16 with triggers, versions, extras per D-14
- AI regeneration with custom prompt per D-15
- Required secrets display per D-20
- Overview Stage 04 active when cicd_config exists per D-24
- All 15 user decisions (D-01 through D-24) implemented
- All 4 requirements (CICD-01 through CICD-04) covered
</success_criteria>

<output>
Create `.planning/phases/05-ci-cd-pipeline-generation/05-03-SUMMARY.md` when done
</output>
